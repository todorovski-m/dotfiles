# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:monospace 11
#font pango:DejaVu Sans Mono 11, FontAwesome 11
font pango:FontAwesome 11
#font pango:Ubuntu Mono 11

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# workspace names
# 1: terminal, 2: www, 3: email, 4: code, 5: files, 6: math, 7: telegram
set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+q kill
bindsym $alt+F4 kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
bindsym $mod+r exec ~/.config/scripts/launcher.sh
bindsym $mod+Shift+w exec ~/.config/scripts/launcher.sh window
# Print Screen
bindsym Print exec ~/.config/rofi/adi1090x/applets/bin/screenshot.sh
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# gaps
smart_gaps on
gaps inner 10
gaps outer 0

# borders (setting border style to pixel eliminates title bars)
default_border pixel 1
# default_border normal 2

# class                 border  backgr. text    indicator child_border
client.focused          #4c7899 #285577 #ffffff #2e9ef4   #FF8C00
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+n move workspace to output next

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Close all windows on workspace 10 (name stored in $ws10)
bindsym $mod+Ctrl+0 [workspace=$ws10] kill

# switch to next/previous workspace
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# switch between the current and the previously focused workspace
bindsym $mod+b workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
bindsym $mod+Ctrl+Right resize shrink width 5 px or 5 ppt
bindsym $mod+Ctrl+Up resize grow height 5 px or 5 ppt
bindsym $mod+Ctrl+Down resize shrink height 5 px or 5 ppt
bindsym $mod+Ctrl+Left resize grow width 5 px or 5 ppt

bindsym $mod+w exec firefox
bindsym $mod+e exec thunderbird
bindsym $mod+Shift+p exec $HOME/programs/vscode/bin/code
bindsym $mod+f exec caja
bindsym $mod+t exec $HOME/programs/Telegram/Telegram
bindsym $mod+z exec xdg-open `cat $HOME/Dropbox/Mirko/bookmark`
bindsym $mod+g exec $HOME/.config/greek/greek.sh
bindsym $mod+c exec foliate ~/win-data/knigi/chess/Irving\ Chernev\ -\ Logical\ Chess\ -\ Move\ By\ Move.epub
bindsym $mod+Shift+c exec $HOME/Dropbox/Mirko/Razno/chess/chernev-open.sh
bindsym $mod+l exec $HOME/.config/scripts/predavanja.sh
bindsym $mod+m exec $HOME/.config/rofi/rofi-network-manager/rofi-network-manager.sh
bindsym $mod+Shift+t exec $HOME/.config/scripts/two_monitors.py
# make this link: ln -s ~/Dropbox/Mirko/dropbox.py ~/.local/bin/dropbox
exec --no-startup-id dropbox start
# polkit authetication agent (needed for packages install)
# install polkit and polkit-gnome
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Num lock
exec_always /usr/bin/numlockx on

# Assign programs to workspaces
# use xprop (sudo apt install xprobe) to find program class
assign [class="kitty"] $ws1
assign [class="firefox"] $ws2
assign [class="Google-chrome"] $ws2
assign [class="thunderbird"] $ws3
assign [class="Emacs"] $ws4
assign [class="Code"] $ws4
assign [class="Caja"] $ws5
assign [class="Pdfpc"] $ws5
assign [class="Octave"] $ws6
assign [class="MATLAB R2016b"] $ws6
assign [class="Telegram"] $ws7

# Programs that start as floating
for_window [class="Weather"] floating enable
for_window [class="Matplotlib"] floating enable
for_window [class="matplotlib"] floating enable

# Change the keyboard layouts
# https://superuser.com/questions/1147320/how-to-change-keyboard-layout-in-i3
# exec_always setxkbmap -layout us,mk -option grp:alt_shift_toggle

# sudo cp ~/Dropbox/Mirko/Razno/linux-settings/ru /usr/share/X11/xkb/symbols/ru
# see the end of ~/Dropbox/Mirko/Razno/linux-settings/ru
# modify /usr/share/X11/xkb/rules/evdev.xml
exec_always setxkbmap -layout "us,mk,ru" -variant ",,mkd" -option grp:alt_shift_toggle
bindsym $mod+Ctrl+m exec xkb-switch -s "mk"
bindsym $mod+Ctrl+e exec xkb-switch -s "us"
bindsym $mod+Ctrl+r exec xkb-switch -s "ru(mkd)"

# https://faq.i3wm.org/question/239/how-do-i-suspendlockscreen-and-logout/
# Shutdown menu
# in ~/.local/bin do this: ln -s ~/.config/i3/i3exit
#set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
set $mode_system System (l) lock, (e) logout, (s) suspend, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
     bindsym l exec --no-startup-id i3exit lock, mode "default"
     bindsym e exec --no-startup-id i3exit logout, mode "default"
     bindsym s exec --no-startup-id i3exit suspend, mode "default"
     #bindsym h exec --no-startup-id i3exit hibernate, mode "default"
     bindsym r exec --no-startup-id i3exit reboot, mode "default"
     bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

     # back to normal: Enter or Escape
     bindsym Return mode "default"
     bindsym Escape mode "default"
}
bindsym $mod+x mode "$mode_system"

# Set resolution and display wallpaper
exec_always --no-startup-id $HOME/.config/scripts/set_resolution.sh; $HOME/.fehbg
bindsym $mod+Shift+b exec sxiv -t $HOME/Pictures/Wallpapers

exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id /usr/bin/nm-applet

# Windows compositor
# config file: ~/.config/picom/picom.conf
# https://www.reddit.com/r/i3wm/comments/s59e7n/weird_picom_issues/
exec_always --no-startup-id sleep 3 && picom -b

set $mode_password (c) copy password, (t) type login and password
mode "$mode_password" {
    bindsym c exec "$HOME/.config/scripts/passScript.sh", mode "default"
    bindsym t exec "$HOME/.config/scripts/passScript.sh --type", mode "default"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+p mode "$mode_password"

bindsym $mod+y move position center
bindsym $mod+Shift+i move scratchpad
bindsym $mod+Ctrl+i exec $HOME/.config/scripts/rofi_scratchpad.sh
bindsym $mod+i scratchpad show

# Use octave as calculator
for_window [instance="calculator"] floating enable
for_window [instance="calculator"] resize set 800 600
for_window [instance="calculator"] border pixel 5
for_window [instance="calculator"] move position center
bindsym $mod+k exec kitty --class calculator -o font_size=18 -e octave -q

# Launch Conky
exec_always --no-startup-id $HOME/.config/conky/launch-conky.sh

# i3-layouts
set $i3l 2columns to workspace $ws1
exec_always $HOME/anaconda3/bin/i3-layouts

# screen magnification
bindsym $alt+z exec boomer
